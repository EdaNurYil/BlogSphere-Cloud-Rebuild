<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
    <link rel="stylesheet" href="/css/main.css">
    <title>Article</title>
</head>
<body>
    <% if (errorMessage) { %>
        <div class="alert alert-danger"><%= errorMessage %></div>
    <% } else { %>
        <div class="container">
            <h1><%= article.title %></h1>
            <p><strong>Category:</strong> <%= article.categoryName %></p>
            <p><strong>Author:</strong> <%= article.author %></p>
            <p><strong>Date:</strong> <%= new Date(article.date + 'T00:00:00').toLocaleDateString() %></p>
    
            <% if (article.featureImage) { %>
                <img src="<%= article.featureImage %>" alt="Feature Image" class="img-fluid">
            <% } %>
    
            <p><%= article.content %></p>
        </div>
    <% } %>
</body>
</html>